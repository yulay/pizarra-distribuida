public class HiloReceptor extends Thread
{
    @Override
    public void run()
    {
        /*Thread hiloActual = Thread.currentThread();
        Trama obj = null;
        
        while(hilo == hiloActual)
        {
            obj = recibirDatagrama();
            
            vtr = obj.getVector();
            jPanel1.setVector(vtr);
            jTextArea1.setText(obj.getString());
            jTextArea1.requestFocus();
            jTextArea1.selectAll();
            jTextArea1.select(jTextArea1.getSelectionEnd(), jTextArea1.getSelectionEnd());
            //jTextArea1.setSelectionEnd(jTextArea1.getText().length());
            jPanel1.repaint();
        }*/
    }
}